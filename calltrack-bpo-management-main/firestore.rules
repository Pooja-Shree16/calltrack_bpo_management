
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Basic authenticated access for all collections used in the BPO app
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
